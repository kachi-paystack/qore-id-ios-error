import SwiftUI
import QoreIDSDK

struct ContentView: View {
    var body: some View {
        VStack {
            Button("Open Qore ID") {
                
            }
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
